import { Table } from 'antd'
const Requests = () => (
  <Table
    bordered
    // columns={columns}
    // dataSource={data}
    footer={() => 'Footer'}
    title={() => 'Danh sách yêu cầu nghỉ phép'}
  />
)
export default Requests
